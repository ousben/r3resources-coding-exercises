function uncamelize(string, separator) {
  let words = [];
  for(let i = 0; i < words.length; i++) {
    let word = "";
    if(string[i] === string[i].toUpperCase()) {

    } else {
      
    }
  }
}